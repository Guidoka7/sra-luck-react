"use client";

/**
 * Mantido apenas por compatibilidade com imports existentes.
 * Os comprovantes agora aparecem exclusivamente dentro das parcelas pagas.
 */
export function ComprovantesClienteActions() {
  return null;
}
