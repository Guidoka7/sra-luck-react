/**
 * Logo da Sra. Luck embutida no próprio bundle (WebP 480px, ~25 KB, gerada a
 * partir de public/brand/sra-luck-logo.png). Por vir junto com o código, ela
 * aparece no mesmo instante que a página, sem uma segunda requisição.
 */
import logo from "./sra-luck-logo.webp?inline";

export const LOGO_SRC = logo;
import mark from "./sra-luck-mark.webp?inline";

/** Símbolo da marca (WebP 128px, embutido) para telas de espera. */
export const MARK_SRC = mark;
